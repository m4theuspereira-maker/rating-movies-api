{
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig-build.json"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "eslint": "^8.5.0",
    "jest": "^27.4.5",
    "jest-mock-extended": "^2.0.4",
    "ts-jest": "^27.1.2",
    "typescript": "^4.5.4"
  }
}
